ExUnit.start()

defmodule User do
  defstruct name: "Shahryar"
end

# `ConditionalFieldValidatorTestValidators` moved to
# `test/support/validators.ex` so fixture modules in
# `test/support/fixtures/extracted/` can compile against it.
